#ifndef HELPERS_H 
#define HELPERS_H 

// ---- Helper functions ----

void swap(int *xp, int *yp); //Swap two values 
void ShuffleArray(int *array, int size); //Shuffles array
void FillArrayRandom(int *array, int size); //Fills array with random values from 0 to 100, without duplicates

#endif
